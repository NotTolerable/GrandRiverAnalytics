SECRET_KEY=change-me
ADMIN_PASSWORD=researchadmin
# Alternatively provide ADMIN_PASSWORD_HASH for a pre-hashed value
BASE_URL=https://example.com
DATABASE=grandriver.db
# Optionally set a persistent filesystem location for SQLite or provide DATABASE_URL
#DATABASE_PATH=/var/data/grandriver.db
#DATABASE_URL=sqlite:////var/data/grandriver.db
# Where to write a CSV backup of posts after each change
#REPORTS_CSV_PATH=/var/data/reports_backup.csv
TINYMCE_API_KEY=
# Optionally override the TinyMCE script source (useful for self-hosted bundles)
TINYMCE_SCRIPT_URL=
# Provide either a kit ID or full URL to load Adelle Sans Thin from Adobe Fonts
ADOBE_FONTS_KIT_ID=
ADOBE_FONTS_URL=
